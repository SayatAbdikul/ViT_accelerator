# Runtime dependencies for the TACCEL Python toolchain.
# Pinned to >= the version exercised in CI so patch-level updates are still
# accepted but breaking changes don't sneak in.

# Core numerical stack
numpy>=2.0,<3
scipy>=1.13,<2

# Image / network IO (for the dataset loaders and the HuggingFace processor)
Pillow>=10,<13
requests>=2.30,<3

# Model loading and the HuggingFace DeiT processor used by the golden flow
torch>=2.0,<3
# transformers 5.9.0 renamed ViT state_dict keys (vit.encoder.layer.X.* →
# vit.layers.X.*, attention.attention.{query,key,value} → attention.{q,k,v}_proj,
# intermediate.dense → mlp.fc1, output.dense → mlp.fc2). Our graph_extract,
# seq_tiling pass, and the compile_w8a16/w8a32 state-dict loops are wired
# to the legacy names, so we pin below 5.9 until those call sites adopt a
# remapping layer. See CI failure: tests/test_w8a{32,16}_compile.py KeyError on
# 'vit.encoder.layer.0.layernorm_before.weight' under transformers 5.9.
transformers>=4.40,<5.9
datasets>=2.18,<5

# ─── Test / development dependencies ──────────────────────────────────────────
# Pin pytest to the major exercised in CI. cocotb-bus needs cocotb >= 1.7.
# Verilator is not a Python dependency — install via the OS package manager
# (apt / brew / dnf) or build from source; the cocotb tests select it via
# `make -C rtl/cocotb SIM=verilator`.
pytest>=8,<10
cocotb>=1.7,<2
cocotb-bus>=0.3,<1
